// Firebase is temporarily disabled while establishing the core training flow.
// Re-enable firebase_core/analytics/crashlytics/messaging in pubspec.yaml
// and restore the full firebase_options.dart when adding analytics (Phase 17+).
